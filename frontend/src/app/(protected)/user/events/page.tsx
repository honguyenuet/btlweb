// Update the import path below if your Events component is located elsewhere
// Update the import path below if your Events component is located elsewhere
// import Events from "../../../components/Events";
import Events from "@/components/events";
import React from "react";

export default function EventsPage() {
  return (
    <div>
      <Events />
    </div>
  );
}
